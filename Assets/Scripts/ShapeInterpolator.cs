using UnityEngine;

public class ShapeInterpolator : MonoBehaviour
{
    // Returns the vertices of the shape between polygon1 and polygon2 at time t
    public static Vector3[] GetShape(Vector3[] polygon1, Vector3[] polygon2, float t)
    {
        return polygon1;
    }
}