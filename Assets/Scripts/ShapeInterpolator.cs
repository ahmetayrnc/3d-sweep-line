using UnityEngine;
using System.Linq;

public class ShapeInterpolator : MonoBehaviour
{
    // Returns the vertices of the shape between polygon1 and polygon2 at time t
    public static Vector2[] GetShape(Vector2[] polygon1, Vector2[] polygon2, float t)
    {
        var temp = polygon1;
        return temp;
    }
}